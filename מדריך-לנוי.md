# נואיקה — איך עובדים על האתר

שניכם עורכים את אותו קובץ משני מחשבים. שתי פקודות מונעות את כל הבלגן.

---

## הכנה — פעם אחת

פותחים חלון פקודות בתיקייה של האתר. ב־GitHub Desktop:
**Repository → Open in Command Prompt**.

```
node scripts/sync.mjs
```

זה מדליק את ההגנות ומוריד את הגרסה העדכנית. ההגנות עוצרות אותך אם הקוד נשבר,
או אם שהר שינה משהו שעדיין לא הורדת.

ההגנות מתקינות את עצמן — גם אם דילגת על השלב הזה, הן ייכנסו לתוקף בפעם
הראשונה שתריצי `sync` או `ship`.

---

## שתי הפקודות

**לפני שמתחילים** — מוריד את מה ששהר עשה:

```
node scripts/sync.mjs
```

**כשמסיימים** — בודק, שומר, משלב עדכונים אם צריך, ושולח:

```
node scripts/ship.mjs "מה שינית"
```

אפשר גם פשוט לכתוב ל־Claude *"תעדכני לי את הקוד"* ו־*"תשלחי את זה לאתר"*.
הוא יריץ את אותן פקודות.

---

## סדר העבודה

```
node scripts/sync.mjs          מורידים עדכונים
claude                         מבקשים מה שרוצים, בעברית
                               "תפתחי לי את האתר לבדיקה"
node scripts/ship.mjs "..."    שולחים
```

---

## עבודה במקביל

**ברוב המקרים זה פשוט עובד.** אם שינית משהו בעגלה ושהר שינה משהו בדף הניהול,
גיט משלב את שניכם לבד. `ship` מוריד את השינויים שלו, משלב, בודק, ורק אז שולח.

**רק אם שניכם נגעתם באותה שורה בדיוק** תופיע הודעה על התנגשות. תגידי ל־Claude:
*"יש התנגשות בגיט, תפתרי אותה"*. השינויים שלך שמורים ולא הלכו לאיבוד.

הכי פשוט: הודעה קצרה בוואטסאפ לפני שנכנסים לקוד.

---

## אם משהו נעצר

זו לא תקלה — זו ההגנה עובדת.

| ההודעה | מה עושים |
|---|---|
| משהו בקוד שבור — לא שלחתי כלום | *"תקן את מה שהבדיקה מצאה"* ואז `ship` שוב. שום דבר לא הגיע ללקוחות. |
| יש לך שינויים שעוד לא נשמרו | קודם `ship` כדי לשמור, ואז `sync`. |
| שני הצדדים שינו את אותו מקום | *"יש התנגשות בגיט, תפתרי אותה"*. אל תמחקי כלום בעצמך. |

אי אפשר לשבור את האתר לצמיתות — הכל בהיסטוריה ותמיד אפשר לחזור.

---

## הדבר האחד שאסור

**שני דברים אסורים:**

1. **להעלות קבצים דרך האתר של גיטהאב** — הכפתור `Add files via upload`.
2. **ללחוץ Push ב-GitHub Desktop** — הוא לא מוריד קודם את מה שהשני עשה.
   להשתמש רק ב-`ship`. GitHub Desktop מצוין כדי *לראות* מה השתנה.

הוא מחליף את הקובץ כולו בעותק שיש לך במחשב. אם העותק ישן, כל מה שנעשה מאז
נמחק — בלי אזהרה, כי האתר ממשיך להיטען כרגיל. זה כבר קרה ומחק את כל מנגנון
הסנכרון של ההזמנות והמחירים.

הכפתור גם עוקף את כל ההגנות, כי הוא לא עובר דרך המחשב שלך.

**תמיד:** `sync` ← לערוך ← `ship`

---

## רוב הדברים לא צריכים קוד בכלל

דף הניהול: `nuika.co.il/?admin` עם הסיסמה.

משם: מחירים, הוספה ומחיקה של מנות, סימון שאזל, פתיחה וסגירה של הזמנות, מועד
סגירה, קישורי תשלום, וכל המטבח — מזווה, מתכונים, תמחור ותכנון שבועי.

כל שינוי שם מגיע ללקוחות **מיד**, ונשמר גם במחשב וגם בטלפון. בלי `sync`,
בלי `ship`, בלי קוד.

הקוד נחוץ רק לעיצוב, לפיצ'רים חדשים ולתקלות.
