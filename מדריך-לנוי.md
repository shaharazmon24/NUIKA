# נואיקה — מדריך עבודה

## שני סוגי שינויים

**ניהול העסק — בדף הניהול, בלי קוד.**
מחירים, מנות חדשות, אזל מהמלאי, פתיחה וסגירה של הזמנות, מועד סגירה, קישורי
תשלום, וכל המטבח: מזווה, מתכונים, תמחור, תכנון שבועי.

נכנסים ל־`nuika.co.il/?admin` עם הסיסמה. כל שינוי כאן מגיע **מיד** לכל לקוח,
ונשמר גם למחשב וגם לטלפון. אין צורך בכלום מעבר לזה.

**שינויי קוד — עיצוב, פיצ'רים חדשים, תקלות.**
זה החלק שדורש את המדריך הזה.

---

## התקנה — פעם אחת

```bash
git clone https://github.com/shaharazmon24/NUIKA.git
cd NUIKA
node scripts/setup.mjs
```

חשוב: **`git clone`, לא "Download ZIP"**. ZIP הוא עותק מנותק — כל דחיפה ממנו
מוחקת את מה שאחרים עשו.

---

## בכל פעם שעובדים

```bash
git pull
```

לפני הכל, תמיד. מושך את מה שנעשה במקום אחר.

אחר כך פותחים Claude Code בתיקייה ומבקשים מה שרוצים בעברית — "תגדילי את הכפתור
בעגלה", "תוסיפי שדה הערות למשלוח". Claude יערוך את הקוד.

בסוף:

```bash
git add -A
git commit -m "מה שינית"
git push
```

---

## מה קורה אם טועים

הכל מוגן. `git push` נעצר לבד בשני מקרים:

**"הדחיפה נעצרה — יש בעיה באתר"**
משהו בקוד שבור. תבקשי מ־Claude: *"תקן את מה שהבדיקה מצאה"*. שום דבר לא הגיע
ללקוחות.

**"הדחיפה נעצרה — יש עדכונים חדשים בשרת"**
שהר שינה משהו בינתיים. הריצי `git pull`, ואם Claude מדווח על התנגשות — תבקשי
ממנו לפתור אותה. אל תדרסי.

---

## הדבר האחד שאסור

**לעולם לא להעלות קבצים דרך האתר של גיטהאב** — הכפתור
"Add files via upload".

זה מחליף את הקובץ כולו בעותק שיש לך במחשב. אם העותק הזה ישן, כל מה שנעשה מאז
נמחק — ובלי שום אזהרה. האתר ממשיך להיטען כרגיל, אז לא רואים את זה עד שלקוח
נתקל בתקלה.

זה כבר קרה פעם אחת ומחק את כל שכבת הסנכרון של הנתונים.

**הדרך הנכונה תמיד:** `git pull` → לערוך → `git push`.

---

## לפני שמתחילים לעבוד על הקוד

תגידו אחד לשני. כל האתר יושב בקובץ אחד (`index.html`), אז אם שניכם עורכים
במקביל תהיה התנגשות. זה פתיר, אבל עדיף פשוט לא להיקלע לזה.

---

## אם משהו נשבר באתר החי

```bash
git log --oneline -5
git revert <המזהה של הקומיט הבעייתי>
git push
```

`revert` מבטל שינוי בלי למחוק היסטוריה — תמיד אפשר לחזור.

יש גם גיבוי מלא בתגית `v1.0-stable`.
