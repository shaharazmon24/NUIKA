# נואיקה — מדריך עבודה על הקוד

מדריך מלא ומעוצב: שהר שלח לך קישור בוואטסאפ. זה העותק המקוצר, ששמור בתוך
הפרויקט עצמו.

---

## קודם כל: אי אפשר לשבור לצמיתות

שלוש רשתות ביטחון עובדות לבד:

- **בדיקה אוטומטית** — קוד שבור פשוט לא נשלח. הלקוחות לא רואים כלום.
- **חסימת דריסה** — אי אפשר לשלוח בלי לקלוט קודם את מה ששהר עשה.
- **ביטול** — כל שינוי שיצא לאתר אפשר להחזיר בפקודה אחת.

---

## הכנה — פעם אחת

פותחים את החלון השחור: GitHub Desktop → **Repository** → **Open in Command
Prompt**. ואז:

```
node scripts/sync.mjs
```

מחפשים את אחת השורות: *"הפעלתי את ההגנות על המחשב הזה"* או *"הכל מסונכרן"*.
זהו, ההכנה נגמרה.

אם כתוב ש-`node` לא מוכר — להתקין מ-nodejs.org (הכפתור עם LTS), ואז לסגור
את החלון השחור ולפתוח מחדש.

---

## עוד דבר אחד פעם אחת — כדי שלוח האירועים יעבוד

**זה לא קוד, וזה לא משהו שקלוד יכול לעשות במקומך.** זה נכנס לחשבון שלך,
אז רק את יכולה. לוקח דקה.

**עד שתעשי את זה, לוח האירועים פשוט לא עובד:**

- בדף הניהול, בלשונית **אירועים** — תוכלי להקליד אירוע, אבל **השמירה
  תיכשל**. הוא לא יישמר בשום מקום.
- בעמוד האירועים באתר — הלקוחות יראו **"אין אירועים"**, גם אם הזנת
  עשרה.

זה לא באג ולא משהו שנשבר. פשוט צריך להגיד למסד הנתונים פעם אחת שמותר
לשמור שם אירועים.

### מה לעשות

1. להיכנס ל-**console.firebase.google.com** (עם אותו חשבון גוגל של
   האתר).
2. לבחור את הפרויקט **nuika-5371f**.
3. בתפריט בצד — **Realtime Database**.
4. למעלה, בלשוניות — **Rules**.
5. יש שם ריבוע גדול עם טקסט. **למחוק ממנו הכל.**
6. במקום זה, להדביק את התוכן של הקובץ `firebase-rules.json` שנמצא
   בתיקייה של האתר. (לא בטוחה איפה הוא? תבקשי מקלוד: *"תפתחי לי את
   `firebase-rules.json` ותעתיקי לי את כל התוכן"*.)
7. ללחוץ **Publish**.

זהו. מהרגע הזה השמירה בלשונית אירועים עובדת, והלוח באתר מראה מה שהזנת.

**זה בטוח ולא שובר כלום.** בדקנו — כל שאר החלקים (הזמנות, מנות, מחירים,
מלאי, המטבח) כתובים בקובץ הזה בדיוק כמו שהם עכשיו. הפרסום רק *מוסיף*
את האירועים.

אם את רוצה לוודא שזה תפס — תבקשי מקלוד *"תבדקי אם הכללים של האירועים
פורסמו"*. הוא יודע איך לבדוק.

---

## שלוש הפקודות

```
node scripts/sync.mjs                  לפני שמתחילים
node scripts/ship.mjs "מה שינית"       כשמסיימים
node scripts/undo.mjs                  רק אם משהו יצא לא טוב
```

אפשר גם פשוט לכתוב לקלוד *"תעדכני לי את הקוד"* ו*"תשלחי את זה לאתר"*.

---

## סדר העבודה

1. וואטסאפ לשהר: *"אני נכנסת לקוד"*
2. `node scripts/sync.mjs`
3. `claude` — ולכתוב לו בעברית מה רוצים
4. *"תפתחי לי את האתר לבדיקה"* — לראות לפני שהלקוחות רואים
5. לצאת מקלוד (`exit`)
6. `node scripts/ship.mjs "מה שינית"`
7. וואטסאפ: *"סיימתי"*

---

## איך מדברים עם קלוד

בעברית רגילה. תגידי מה את רוצה שיקרה, לא איך לעשות את זה:

> "הכפתור של העגלה קטן מדי בטלפון, תגדילי אותו"

> "תוסיפי שדה בטופס ההזמנה לשעת איסוף מועדפת"

> "התיאור של הלחם נחתך, תסדרי את זה"

ואם משהו לא ברור — *"לא הבנתי, תסבירי במילים פשוטות"*.

כל עוד לא הרצת `ship`, שום דבר לא הגיע לאתר. אפשר תמיד לומר
*"תחזירי הכל אחורה"*.

---

## אם משהו נעצר

| ההודעה | מה עושים |
|---|---|
| משהו בקוד שבור — לא שלחתי כלום | `claude` → *"תקן את מה שהבדיקה מצאה"* → `ship` שוב |
| יש עדכונים חדשים מהמחשב השני | `node scripts/sync.mjs` |
| יש לך שינויים שעוד לא נשמרו | קודם `ship`, אחר כך `sync` |
| שני הצדדים שינו את אותו מקום | `claude` → *"יש התנגשות בגיט, תפתרי אותה"*. אל תמחקי כלום בעצמך |
| `node is not recognized` | להתקין Node, ואז לסגור ולפתוח את החלון מחדש |
| משהו אחר | לצלם ולשלוח לשהר, או להדביק לקלוד |

---

## לבטל שינוי שכבר יצא לאתר

```
node scripts/undo.mjs
```

מראה מה השינוי האחרון, שואלת אם לבטל, ומחזירה את האתר אחורה תוך דקה.
שום דבר לא נמחק — השינוי שמור בהיסטוריה ואפשר להחזיר אותו.

---

## שלושה דברים שאסור

1. **להעלות קבצים דרך האתר של גיטהאב** — הכפתור `Add files via upload`.
   הוא מחליף את הקובץ כולו ומוחק כל מה שנעשה מאז, בלי אזהרה. זה כבר קרה
   ומחק את כל מנגנון הסנכרון.

2. **ללחוץ Push או Commit ב-GitHub Desktop** — הוא לא מוריד קודם את מה
   ששהר עשה ולא בודק שהקוד תקין. להשתמש רק ב-`ship`. GitHub Desktop מצוין
   כדי *לראות* מה השתנה.

3. **לערוך את הקוד ידנית** — `index.html` הוא קובץ אחד בן 5,000+ שורות
   שהכל בו קשור להכל. תמיד דרך קלוד.

---

## רוב הדברים לא צריכים קוד בכלל

דף הניהול: האייקון "ניהול" במסך הבית, או `nuika.co.il/admin.html`.

משם: מחירים, הוספה ומחיקה של מנות, סימון שאזל, העברת מנה לבנק המנות
וחזרה, פתיחה וסגירה של הזמנות, מועד סגירה, קישורי תשלום, וכל המטבח —
מזווה, מתכונים, תמחור ותכנון שבועי.

כל שינוי שם מגיע ללקוחות **מיד**, מכל מכשיר, בלי `sync` ובלי `ship`.

הקוד נחוץ רק לעיצוב, לפיצ'רים חדשים ולתקלות.
