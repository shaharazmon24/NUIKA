# NUIKA — תדריך מלא לקלוד קוד של נוי

> **קרא את המסמך הזה במלואו לפני שאתה נוגע בקוד, ולפני שאתה עונה לנוי על
> בקשה כלשהי.** הוא נכתב על ידי קלוד קוד שעובד עם שהר, השותף השני לפרויקט,
> כדי ששני הצדדים יעבדו לפי אותם כללים. כל מה שכתוב כאן נובע מדברים שקרו
> באמת ועלו במחיר אמיתי.

---

## 0. הדבר הראשון שאתה עושה — לפני כל שאלה, לפני כל קוד

**תוודא שאתה בתיקייה הנכונה.** תריץ:

```bash
node scripts/status.mjs
```

- אם זה עובד ומראה `✓ התיקייה מחוברת ל-shaharazmon24/NUIKA` — אתה במקום הנכון.
- **אם זה נכשל, או שאין `scripts/`, או שכתוב `❌` — עצור מיד.** אל תכתוב שורת
  קוד אחת. אתה בתיקייה שאינה הפרויקט.

### למה זה קריטי

זה כבר קרה. קלוד קוד נפתח בתיקייה חדשה וריקה, בנה שם פיצ'רים שלמים, ונוי
בדקה אותם ולא הבינה למה שום דבר לא מתנהג כמו האתר שלה. **כל העבודה הייתה
מנותקת מהאתר האמיתי.** התיאור שלה היה: *"הוא כאילו לא מחובר"*. הוא באמת לא
היה.

תיקייה ריקה נראית תקינה לחלוטין מבפנים. אין שום סימן אזהרה. `index.html`
שנוצר מאפס נראה כמו קובץ תקין — הוא פשוט לא האתר.

**סימני היכר שאתה בתיקייה הלא נכונה:**

- אין תיקיית `scripts/`
- אין `CLAUDE.md`
- **אין `shop.html`.** זה הקובץ הגדול — החנות. האמיתי הוא ~430KB, מעל 9,000
  שורות.
- `git remote get-url origin` לא מחזיר `shaharazmon24/NUIKA`
- אין `firebase-rules.json`, אין `CNAME`

> ⚠️ **אל תמדוד את זה על `index.html`.** עד 21.9.2026 החנות **כן** ישבה על
> `index.html`, והשורה הזו אמרה "קטן מ-200KB = תיקייה לא נכונה". מאז
> `index.html` הוא **עמוד הבית עם הסרט**, והוא אמור להיות קטן: ~15KB, כ-280
> שורות. כלומר בתיקייה **הנכונה** מבחן הגודל הישן ייכשל. אם יש לך זיכרון
> מהשם הישן — הוא כבר לא נכון. הקובץ הגדול הוא `shop.html`.

### מה לעשות אם אתה בתיקייה הלא נכונה

1. **אל תמחק כלום.** אם נבנתה שם עבודה, היא עדיין שווה משהו — צריך להעביר
   אותה, לא לאבד אותה.
2. תשאל את נוי איפה נמצאת התיקייה המקורית. סביר שהיא תחת
   `Documents\GitHub\NUIKA` או משהו דומה, כי היא משתמשת ב-GitHub Desktop.
3. אם היא לא יודעת — תשכפל מחדש:

   ```bash
   git clone https://github.com/shaharazmon24/NUIKA.git
   ```

4. תעביר את העבודה שנעשתה בתיקייה הזמנית אל התיקייה האמיתית **ידנית
   ובזהירות**, שינוי אחר שינוי. אל תעתיק קבצים שלמים — זה בדיוק מה שמחק
   פעמיים את כל שכבת Firebase.
5. `node scripts/validate.mjs`, ואז `node scripts/ship.mjs "תיאור"`.

---

## 0.1 מעקב גרסאות — איך יודעים שכולם על אותו דף

```bash
node scripts/status.mjs
```

משווה שלושה מקומות ואומר אם הם זהים:

| מה | מה זה |
|---|---|
| **התיקייה שלך** | מה שאתה עורך עכשיו |
| **גיטהאב** | מה שמשותף לשני האנשים |
| **האתר החי** | מה שהלקוחות באמת רואים |

לכל פרסום נחתמת גרסה אוטומטית בתוך `shop.html` (על ידי `ship.mjs` — **לעולם
אל תערוך את תג `nuika-version` ידנית**). אפשר לראות אותה בשלוש דרכים:

- **בפאנל הניהול** — מתחת לכותרת "לוח ניהול". נוי יכולה להקריא אותה בטלפון.
- **בקונסול של הדפדפן** — `NUIKA_VERSION`, מכל מכשיר כולל טלפון.
- **בפקודה** — `node scripts/status.mjs`.

**תריץ את זה בתחילת כל סשן ולפני כל שליחה.** אם הגרסאות לא תואמות — תטפל בזה
לפני שאתה כותב קוד, אחרת אתה עלול לדרוס עבודה של מישהו אחר.

---

## 1. מה זה הפרויקט הזה

**נואיקה** היא מאפיית בוטיק אמיתית שנוי מנהלת, פעילה בכתובת
**https://nuika.co.il**. זה לא תרגיל ולא אתר הדגמה.

- הזמנות שנכנסות דרך האתר הן **הזמנות אמיתיות של לקוחות משלמים**.
- בבסיס הנתונים יושבים **שמות, טלפונים וכתובות של אנשים אמיתיים**.
- אתר שבור פירושו לקוחות שלא יכולים להזמין ולחם שלא נאפה.

**המשמעות המעשית בשבילך:** אתה לא בסביבת פיתוח. אין staging. כל דבר שנשלח
מגיע ללקוחות תוך דקה. תתנהג בהתאם — בדוק לפני שאתה שולח, ואל תנחש.

---

## 2. עם מי אתה עובד

**נוי** — הבעלים. אופה, מנהלת את העסק. **אין לה רקע טכני.** היא מדברת עברית.
היא לא יודעת גיט, לא יודעת מה זה commit, ולא אמורה לדעת. אל תסביר לה
במונחים טכניים ואל תבקש ממנה להריץ פקודות גיט גולמיות.

**שהר** — המפתח. עובד על אותו קובץ ממחשב אחר, עם קלוד קוד משלו.

**שני אנשים, קובץ אחד, שני מחשבים.** זה מקור כמעט כל הבעיות בפרויקט הזה.
כל מה שכתוב במסמך הזה על סנכרון קיים בגלל זה.

---

## 3. הכלל היחיד שחשוב יותר מכל השאר

### לעולם אל תיתן לנוי להעלות קבצים דרך האתר של גיטהאב

הכפתור נקרא **"Add files via upload"**, והוא נמצא בממשק הווב של
`github.com/shaharazmon24/NUIKA`. הוא נראה תמים לגמרי. הוא **מחליף את הקובץ
כולו** בעותק המקומי שיש לה, וגיט רושם את זה כ-commit רגיל לגמרי — בלי אזהרה,
בלי התנגשות, בלי שום סימן שמשהו נמחק.

**זה כבר קרה פעמיים.**

**פעם ראשונה** — commit `cb5da2f`. מחק מנגנון סנכרון שלם.

**פעם שנייה** — commit `16b3022`, ב-16 באוגוסט 2026. זו הייתה החמורה:

| מה נמחק | התוצאה בפועל |
|---|---|
| כל שכבת Firebase (960 שורות) | האתר חזר לתפריט קשיח מלפני חודשים |
| מנגנון האימות | הכניסה לפאנל חזרה להשוואת סיסמה גלויה בקוד |
| חיבור בסיס הנתונים | הזמנות הפסיקו להישמר בכלל |

התוצאה: במשך יומיים **לקוחות ראו תפריט של 6 מנות שכבר לא קיימות**, בזמן
שהתפריט האמיתי של נוי בבסיס הנתונים כלל 5 מנות אחרות לגמרי. ההזמנות לא נכנסו
לפאנל. נוי לא הצליחה להיכנס לניהול והייתה בטוחה שהסיסמה שלה התקלקלה.

**האתר המשיך להיטען כרגיל כל הזמן הזה.** שום דבר לא נראה שבור. זה בדיוק מה
שהופך את זה למסוכן.

### מה לעשות במקום

```bash
node scripts/sync.mjs                  # לפני שמתחילים לעבוד
node scripts/ship.mjs "מה שינית"       # כשמסיימים
```

**רק שתי הפקודות האלה.** הן מתקינות את ההגנות בהרצה הראשונה, מסרבות לשלוח
קוד שבור, ומסרבות לשלוח כשהמחשב השני מקדים.

### איך לזהות שנוי עומדת לעשות את זה

אם היא אומרת משהו בסגנון:

> *"אני אעלה את הקובץ לגיטהאב"* · *"יש לי עותק של הקובץ במחשב"* ·
> *"אני אגרור את הקובץ לאתר"* · *"שמרתי גרסה אצלי, אני אשים אותה"*

**עצור אותה מיד.** תסביר בעברית פשוטה שהעלאה כזו מוחקת את כל מה שנעשה מאז
שהעותק שלה נשמר, ותציע לעשות את השינוי דרכך במקום.

### אסור גם

- **Push או Commit ב-GitHub Desktop.** הוא לא מוריד קודם את העבודה של שהר
  ולא בודק שהקוד תקין. GitHub Desktop מצוין כדי *לראות* מה השתנה — לא כדי
  לשלוח.
- **`git push` גולמי.** מדלג על כל הבדיקות שקיימות בדיוק בגלל שכבר אבדה
  עבודה.

---

## 4. אם זה כבר קרה — נוהל שחזור

סימנים: פיצ'רים שנעלמו, האתר נראה כמו גרסה ישנה, נוי לא מצליחה להיכנס לפאנל,
או שהתפריט באתר לא תואם למה שהיא רואה בניהול.

**אבחון — קודם כל הכלי, ורק אחריו ידנית:**

```bash
node scripts/status.mjs
git log --oneline -10
```

`status.mjs` כבר יודע מי החנות באתר החי ומשווה את שלוש הגרסאות. אם הוא רץ,
הוא התשובה. הבדיקה הידנית שלמטה היא לגיבוי — ויש בה מלכודת אחת שחייבים
להבין לפני שמריצים אותה.

### 🛑 שתי סיבות שונות ל-0, ורק אחת מהן היא אסון

הבדיקה הידנית סופרת כמה פעמים המילה `firebase` מופיעה בקובץ. **0 יכול להגיע
משתי סיבות שונות לגמרי:**

| מה קרה | המשמעות |
|---|---|
| הקובץ קיים (HTTP 200) ואין בו Firebase | **זה האסון.** השכבה נמחקה. |
| הקובץ לא קיים (HTTP 404) | הקובץ פשוט לא שם. **זה לא אסון.** |

בלי לבדוק את קוד ה-HTTP אי אפשר להבדיל, ו**שתי הטעויות האלה כבר נכתבו כאן**:
פעם `index.html` (שמאז ההחלפה הוא עמוד הבית ואין בו Firebase כלל), ופעם
`shop.html` (שמחזיר 404 בכל רגע שבו ההחלפה עוד לא פורסמה, או אחרי ביטול שלה).
בשני המקרים התוצאה היא 0 על אתר בריא לגמרי, והשורה הבאה במסמך היא נוהל שחזור.

> **אל תסתפק ב"התשובה לא הייתה ריקה".** GitHub Pages עונה על קובץ חסר בדף
> 404 מעוצב שהוא HTML תקין לגמרי — נמדד 9,379 בתים עבור `/shop.html`
> ב-22.9.2026. בדיקת "ריק או לא ריק" תקרא את דף ה-404 הזה כהוכחה שהקובץ
> קיים. `scripts/status.mjs` מסרב לבדיקה כזו בדיוק מהסיבה הזו — ההסבר כתוב
> שם בקוד.

**הבדיקה הידנית, בצורה שלא יכולה לשקר** — שואלת את שני הקבצים, ומדפיסה גם את
קוד ה-HTTP:

```bash
for f in shop.html index.html; do
  code=$(curl -s -o /dev/null -w '%{http_code}' "https://nuika.co.il/$f")
  n=$(curl -s "https://nuika.co.il/$f" | grep -c -i firebase)
  echo "$f  HTTP $code  firebase=$n"
done
```

**איך לקרוא את הפלט:**

- תתייחס **רק** לשורות עם `HTTP 200`. שורה עם 404 לא אומרת כלום.
- **בדיוק אחת** מהשורות עם 200 צריכה להחזיר מספר דו-ספרתי. זו החנות.
- אם אף שורה עם 200 לא מחזירה מספר דו-ספרתי — **זה האסון.** תמשיך לנוהל.

שני הפלטים האלה שניהם בריאים, והם נראים הפוך זה מזה:

```
# לפני שההחלפה פורסמה (נמדד 22.9.2026):
shop.html   HTTP 404  firebase=0     <- לא קיים עדיין. תקין.
index.html  HTTP 200  firebase=54    <- זו החנות.

# אחרי שההחלפה פורסמה:
shop.html   HTTP 200  firebase=54    <- זו החנות.
index.html  HTTP 200  firebase=0     <- עמוד הבית עם הסרט. תקין.
```

בתיקייה המקומית אותה שאלה נענית בלי HTTP בכלל:

```bash
grep -l "firebase.initializeApp" *.html   # זו החנות, איך שלא תיקרא
```

**שחזור — לפי הסדר הזה בדיוק:**

1. **קודם כל תבדוק מה נמחק ומה נוסף.** אף פעם אל תשחזר בעיוורון:

   ```bash
   git log -S "<שם פונקציה שנעלמה>" --oneline --all
   ```

2. **תחפש עבודה אמיתית בתוך ההעלאה השוברת.** בפעם השנייה, ההעלאה של נוי
   הכילה פיצ'ר שלם — "הזמנה ידנית" — שלא היה בשום גרסה אחרת. אילו היינו
   משחזרים בעיוורון, הוא היה נמחק.

3. **תשחזר את הגרסה התקינה, ואז תשתיל בחזרה את מה שהיה ייחודי בהעלאה.**

4. `node scripts/validate.mjs` — חייב לעבור במלואו.

5. `node scripts/ship.mjs "תיאור"`.

6. **תאמת על האתר החי**, לא רק מקומית.

> **הכלל הקדוש:** בהתנגשות ב-`shop.html` — **לעולם אל תפתור בלקיחת צד אחד
> שלם.** העבודה של שני הצדדים אמיתית. תמזג ידנית, שורה שורה.

---

## 5. מה אסור לך לעשות — הנחיות ישירות אליך

1. **אל תריץ `git push`, `git commit`, `git merge` או `git pull` גולמיים.**
   רק `sync.mjs` ו-`ship.mjs`.
2. **אל תשלח בלי להריץ `validate.mjs` ולראות שהוא עובר.**
3. **אל תיגע ב-`CNAME`.** מחיקה או עריכה שלו מנתקת את הדומיין ומורידה את
   החנות. הקובץ מכיל שורה אחת: `nuika.co.il`.
4. **אל תמחק את `nuika/_seeded`** בבסיס הנתונים. הוא הסמן שמונע כתיבה מחדש
   של ברירות מחדל על נתונים אמיתיים.
5. **אל תמחק ואל תשנה נתונים בבסיס הנתונים** בלי שנוי ביקשה במפורש. שם
   יושבות הזמנות אמיתיות.
6. **אל תדווח על הצלחה שלא אימתת.** אם בדיקה נכשלה — תגיד את זה בפירוש.
7. **אל תשנה את מודל האבטחה** (Firebase Auth, `nuika/admins`, הכללים) בלי
   לתאם עם שהר. שינוי שגוי שם נועל את נוי מחוץ לעסק שלה.
8. **אל תוסיף שלב build, framework או תלויות.** הפרויקט הוא קובץ אחד בלי
   build בכוונה.

---

## 6. מה אתה חייב לוודא שנוי עושה

- **לפני עבודה** — שהיא הריצה `node scripts/sync.mjs`. אם לא, תריץ בעצמך.
- **לפני שליחה** — ש-`validate.mjs` עבר.
- **אחרי שליחה** — שהיא יודעת שהשינוי יגיע לאתר תוך כדקה.
- **תיאום עם שהר** — כדאי שתודיע לו בוואטסאפ לפני שהיא נכנסת לקוד ואחרי
  שסיימה. תזכיר לה.
- **אם היא מבקשת לשנות סיסמה או הרשאות** — תפנה אותה לקישור "שכחתי סיסמה"
  במסך הכניסה, לא לעריכת קוד.

---

## 7. רוב הבקשות של נוי לא צריכות קוד בכלל

**זה החלק שיחסוך הכי הרבה סיכון.** לפני שאתה נוגע ב-`shop.html`, תבדוק אם
מה שהיא רוצה כבר קיים בפאנל הניהול.

נוי מנהלת מהפאנל, בלי שורת קוד אחת:

| מה | איפה |
|---|---|
| מחירים | פאנל → מנות |
| הוספה, עריכה ומחיקה של מנות | פאנל → מנות |
| תמונות מוצרים | פאנל → מנות (העלאה ישירה) |
| סימון "אזל" | פאנל → מנות |
| הסתרת מנה לבנק המנות וחזרה | פאנל → מנות |
| פתיחה וסגירה של הזמנות | פאנל → הגדרות |
| מועד סגירת ההזמנות | פאנל → הגדרות |
| קישורי Bit ו-PayBox | פאנל → הגדרות |
| ניהול הזמנות, סטטוסים, ארכיון | פאנל → הזמנות |
| הזמנה שהתקבלה בטלפון | פאנל → הזמנות → "הזמנה ידנית" |
| רשימת אפייה והדפסה | פאנל → הזמנות |
| ייצוא הזמנות ל-CSV | פאנל → הזמנות |
| מזווה, מתכונים, תמחור, תכנון שבועי | פאנל → מטבח |

**כל שינוי שם מגיע ללקוחות מיד, מכל מכשיר, בלי `sync` ובלי `ship`.**

**גישה לפאנל:** האייקון "ניהול" במסך הבית, או `nuika.co.il/admin.html`.

אם נוי מבקשת שינוי קוד למשהו מהרשימה — **תעצור ותגיד לה שזה כבר קיים בפאנל,
ותסביר איפה.** שינוי קוד מיותר הוא סיכון מיותר.

**קוד נחוץ רק ל:** עיצוב ופריסה, פיצ'רים חדשים, ותקלות.

---

## 8. ארכיטקטורה

**בלי שלב build.** מאז ההחלפה של 21.9.2026 יש לאתר שני עמודי כניסה:
`index.html` הוא **עמוד הבית עם הסרט** (מה ש-nuika.co.il מגיש, ~280 שורות),
ו-`shop.html` הוא **החנות** — קובץ אחד בן מעל 9,000 שורות שמכיל את כל ה-HTML,
ה-CSS וה-JavaScript שלו. Tailwind ו-Firebase נטענים מ-CDN. עברית RTL עם מתג
לאנגלית שמונע על ידי המאפיינים `lang-content="he"` ו-`lang-content="en"`.

> **אם יש לך זיכרון שהחנות היא `index.html` — הוא מלפני ההחלפה.** היום זה שם
> של עמוד הבית. אל תזהה את החנות לפי שם — תזהה אותה לפי תוכן:
> `grep -l "firebase.initializeApp" *.html`
>
> הכלים (`status.mjs`, `ship.mjs`, `validate.mjs`, `enable-deploy-gate.mjs`)
> עושים את זה בשני שלבים, וכדאי לדעת מה בדיוק: הם **מוצאים** את הקובץ לפי שם
> (`shop.html`, ואם אין — `index.html`; שני השמות כתובים בקוד ב-`shopFile()`),
> ואז **מוודאים לפי תוכן** שהוא באמת החנות. האימות הוא מה שהציל את ההחלפה —
> בלעדיו הם היו ממשיכים לקרוא את `index.html` ולבדוק, לחתום ולפרסם מול עמוד
> הסרט. אבל זו לא עצמאות משם: שינוי שם שלישי יעצור את כל הארבעה בהודעה
> `neither shop.html nor index.html exists`, וצריך לערוך את `shopFile()` בכל
> ארבעת העותקים לפני שינוי כזה.

קבצים נוספים:

| קובץ | תפקיד |
|---|---|
| `index.html` | עמוד הבית — הסרט. מסך אחד, בלי גלילה. |
| `shop.html` | **החנות.** הזמנות, תשלום, מלאי, מטבח, כספים, ניהול. |
| `story.html` `gallery.html` `contact.html` `events.html` | עמודי תוכן, חולקים `site.css` ו-`site.js` |
| `admin.html` | דלת כניסה נפרדת לניהול — מפנה ל-`./shop.html?admin` |
| `sw.js` | Service Worker |
| `manifest.json` | הגדרות PWA |
| `CNAME` | הדומיין — **לא לגעת** |
| `firebase-rules.json` | כללי האבטחה של בסיס הנתונים |
| `scripts/` | כלי העבודה |
| `SECURITY.md` | תיעוד הקמת האבטחה |
| `CLAUDE.md` | כללי העבודה — קרא גם אותו |

### מפת הנתונים ב-Firebase

**כל מה שמשותף יושב ב-Firebase Realtime Database** תחת `nuika/`:

| נתיב | תוכן |
|---|---|
| `nuika/products` | תפריט, מחירים, אזל, מכסות מלאי, תגיות |
| `nuika/orders` | כל ההזמנות, לפי חותמת זמן |
| `nuika/settings` | `ordersOpen`, `bitLink`, `payboxLink`, `deadline` |
| `nuika/events` | לוח האירועים של נוי — רשומה לכל אירוע: `date` (בפורמט `YYYY-MM-DD`), `title`, ואם יש גם `place`, `time`, `body`, `ctaLabel`, `ctaText`, `created`. קריאה ציבורית, כתיבה למנהלים בלבד. **דורש פרסום כללים ידני — ראה 8.1** |
| `nuika/stockUsed` | מונה מלאי ציבורי לפי מחזור |
| `nuika/admins` | UID של מנהלים — נכתב ידנית בקונסול בלבד |
| `nuika/kitchen/pantry` | מרכיבים ומחירים |
| `nuika/kitchen/recipes` | מתכונים עם משקלי מרכיבים |
| `nuika/kitchen/weeklyPlan` | `{recipeId: quantity}` |
| `nuika/_seeded` | סמן זריעה חד-פעמי — **לעולם לא למחוק** |

**`localStorage` הוא רק לנוחות מקומית** של המכשיר: העגלה של הלקוח, השם
והטלפון שלו, וההזמנה האחרונה שלו. **שום דבר שנוי מנהלת לא שייך לשם** — זה לא
יגיע לאף מכשיר אחר.

---

## 8.1 פעולה ידנית אחת שנוי חייבת לעשות — כללי `nuika/events`

**זה חוסם את לוח האירועים לגמרי, ושום קוד לא יתקן את זה.**

`firebase-rules.json` בריפו הוא הקובץ שאנחנו עורכים — אבל **הוא לא מה
שמסד הנתונים אוכף**. מסד הנתונים אוכף את מה שהודבק לאחרונה בקונסול של
Firebase. הצומת `nuika/events` נמצא בקובץ אבל **עדיין לא פורסם**.

עד שמישהו יפרסם, שני הצדדים של הפיצ'ר מתים:

- לשונית **אירועים** בדף הניהול (`?admin`) **לא מצליחה לשמור** — כל כתיבה
  חוזרת עם `PERMISSION_DENIED`;
- הלוח הציבורי ב-`events.html` **מראה "אין אירועים"** לא משנה מה נוי הזינה.

### איך לבדוק אם זה כבר נעשה

```bash
curl -s -o /dev/null -w '%{http_code}\n' \
  'https://nuika-5371f-default-rtdb.firebaseio.com/nuika/events.json'
```

`200` — הכללים פורסמו, הכל עובד. `401` — עדיין לא, והלוח מת.

### מה להגיד לנוי

זה לא משהו שאתה יכול לעשות בשבילה — זה דורש כניסה לחשבון שלה. אם הבדיקה
מחזירה `401`, תגיד לה בפירוש שהלוח לא יעבוד עד שהיא תעשה את זה, ותלווה
אותה שלב-שלב:

1. להיכנס ל-[קונסול Firebase](https://console.firebase.google.com/) ←
   פרויקט **nuika-5371f** ← **Realtime Database** ← לשונית **Rules**.
2. למחוק את כל מה שכתוב שם, ולהדביק במקום את התוכן של
   `firebase-rules.json` מהריפו.
3. ללחוץ **Publish**.

**זה בטוח.** הכללים החיים הושוו לקובץ שבריפו והם זהים בכל צומת אחר —
הזמנות, מוצרים, הגדרות, מלאי והמטבח לא משתנים בכלל. הפרסום רק **מוסיף**
את `events`.

ההנחיה הזו כתובה גם ב-`מדריך-לנוי.md` בשפה שלה. תפנה אותה לשם.

---

## 9. המלכודות — כל אחת מהן כבר עלתה במחיר

**זריעה (seeding).** ברירות מחדל נכתבות בדיוק פעם אחת, בשמירה של
`nuika/_seeded`, ורק לתת-עצים שלא קיימים. **לעולם אל תזרע מתוך מאזין
`.on('value')`** — עץ ריק שם פירושו "נוי מחקה הכל", לא "הרצה ראשונה". טעות
כאן מחזירה לחיים מנות ומתכונים שנמחקו.

**לעולם אל תשמור לפני שהמאזין טען.** `PANTRY`, `RECIPES` ו-`PRODUCTS` ריקים
עד ש-Firebase עונה. שמירה בחלון הזה כותבת ריקנות או ברירות מחדל על נתונים
אמיתיים — זה מחק את המזווה בכל פתיחה במצב לא מקוון. הדגלים `kitchenReady()`,
`_kitchenLoaded` ו-`_productsLoaded` קיימים בדיוק בשביל זה. תמשיך להשתמש בהם.

**העדף כתיבה לפי מפתח.** `db.ref('nuika/kitchen/pantry').set(obj)` מחליף את
כל תת-העץ ומשמיד כל מה שהמכשיר השני שינה באותו סבב. נוי עובדת מטלפון וממחשב
נייד. תכתוב `db.ref('nuika/kitchen/pantry/' + id).set(item)` במקום.

**לכל כתיבה צריך `.catch(fbError)`.** בעבר כתיבות נכשלו בשקט בזמן שהממשק
דיווח על הצלחה.

**תמיד `esc()` על טקסט מלקוחות.** שם, הערות וכתובת נכנסים לפאנל דרך
`innerHTML`. בלי escaping, הערת הזמנה מתוכננת מריצה JavaScript שרירותי מול
בסיס הנתונים החי בסשן של נוי.

**ה-Service Worker.** `sw.js` הוא network-first עבור HTML כדי שתיקונים תמיד
יגיעו לאנשים. **תעלה את `CACHE` בכל שינוי שלו.** הוא לעולם לא יחזור
ל-cache-first — הגרסה ההיא נעלה כל מבקר לעותק ישן לנצח.

**הצהרות פונקציה מורמות (hoisted).** אל תעטוף פונקציה כ-
`const orig = fn; function fn() { orig(); }` — זה נכנס לרקורסיה אינסופית.
תערוך את הגוף ישירות.

**האתר מוגש משורש הדומיין** (nuika.co.il), מאחורי Cloudflare. תשמור על נתיבי
נכסים יחסיים. Cloudflare חייב להישאר במצב SSL **Full**, אף פעם לא
Full (strict) — תעודת המקור היא `*.github.io` של גיטהאב, ומצב strict דוחה
אותה.

---

## 9.1 המטבח — קרא את זה לפני שאתה נוגע בו

נוי ניסתה להוסיף פוד-קוסט למוצר מורכב ונתקלה בכך שאי אפשר להוסיף מרכיב:
*"לוחצת, מתחילה להקליד — החלון קופץ, לא מקליד, לא מוסיף."* קלוד שלה חיפש
את הבאג שלוש פעמים ולא מצא — **כי הבאג לא היה בקוד של הוספת המרכיב.**

**מה זה היה באמת:** מאזין Firebase על `nuika/kitchen` רשם מאזין `focusout`
חדש בכל עדכון מהשרת. כל הקשה שגרמה לכתיבה חזרה כעדכון, וכל עדכון הערים עוד
ריענון. ברגע שהיא עברה משדה השם לשדה הגרמים — **כל הריענונים שהצטברו רצו
בבת אחת**, `switchKitchenTab` דרס את ה-HTML של הטבלה, והשורה שהוקלדה למחצה
נבנתה מחדש מנתוני שרת שמעולם לא הכילו אותה.

**תוקן ב-18.8.2026.** מאומת: 25 עדכוני שרת ברצף → ריענון אחד; חלון עריכה
פתוח → אפס ריענונים, והריענון נדחה ולא אובד.

### הכללים שנובעים מזה — הם מחייבים אותך

1. **לעולם אל תקרא ל-`switchKitchenTab` ישירות ממאזין.** תקרא
   `requestKitchenRepaint()`. הוא מחכה שהפאנל יהיה פנוי.
2. **לעולם אל תרנדר תת-מסך חדש של המטבח לתוך `#admin-panel`** בלי לעבור דרך
   אותו מנגנון. כל `innerHTML` על מיכל שמכיל שדה בפוקוס יהרוס אותו.
3. **אל תקשור כתיבה ל-Firebase ל-`oninput`.** אם חייבים — עם debounce, כמו
   ב-`savePlanQty`. הדפוס הזה הוא בדיוק מה שנוי העתיקה בטעות.
4. **כתיבות במטבח הן פר-מפתח.** `saveRecipe_(r)` ו-`savePantryItem_(p)`.
   **אין יותר `savePantry()` או `saveRecipes()`** — הן דרסו תת-עץ שלם. מחיקה
   חייבת לכתוב `null` במפורש.
5. **אל תסמוך על `parseFloat` למשקלים.** מקלדת עברית מייצרת `0,5` והוא מחזיר
   `0`. השורה נזרקה בשקט. עכשיו יש הודעה במקום.

### הפיצ'ר שנוי רוצה — ולמה הוא עדיין חסום

פוד-קוסט למוצר מורכב (עוגה = בצק בריוש + קרם שוקולד + קרם שקדים). זה **לא**
פיצ'ר קטן. ארבעה חסמים אמיתיים, לפי הסדר:

1. **אין קשר בין מוצר למתכון.** `nuika/products` ו-`nuika/kitchen/recipes`
   הם עצים נפרדים עם סוגי מפתח שונים (מספר מול מחרוזת). הקישור היחיד בקוד
   הוא **השוואת שם בעברית** — נוי משנה שם מוצר, והקשר נשבר בשקט.
2. **`yieldWeightG` הוא דקורטיבי.** נשמר, מוצג, ו**לא משתתף באף חישוב**.
   בלעדיו "חצי מתכון" חסר משמעות — עלות מחושבת רק לפי `yieldCount`, שהוא
   מספר שלם. לפני שהפיצ'ר נבנה, נוי חייבת למלא משקל אמיתי לכל מתכון.
3. **`calcIngCost` יודע לתמחר רק פריטי מזווה.** מתכון-בן לא יימצא, יחזיר
   `null`, וייספר כ"מרכיב ללא מחיר" — כלומר **המוצר יתומחר באפס**. זו הסיבה
   שהוספת מתכון שלם לא עבדה לה.
4. **אין הגנה מפני מעגליות.** מתכון שמכיל את עצמו יתקע את כל לשונית המטבח
   בלולאה אינסופית, בלי הודעת שגיאה — מסך מת.

**כשתבנה את זה:** צריך `kind: 'pantry' | 'recipe'` על שורת מרכיב, חישוב
רקורסיבי עם `seen` Set, **ותיקון בשני המקומות** — `calcRecipeCost` וגם
`recalcEditorCosts`, שהן שתי מימושים נפרדים של אותה מתמטיקה.

### מע"מ — אל תיגע לפני שנוי עונה

`calcIngCost` מכפיל ב-`(1 + (p.vat || 0.17))`. שים לב ל-`||`: **מע"מ 0 בלתי
ניתן לביטוי** — הוא נופל חזרה ל-17%. באותה פונקציה, שורה אחת קודם, יש
`typeof` נכון על המחיר עם הערה שמסבירה בדיוק למה. אותה טעות חזרה על עצמה.

אבל הבעיה הגדולה יותר אינה 17 מול 18: **לא ידוע אם `pricePerKg` הוא לפני
מע"מ או אחריו**, וסביר שהמזווה מערבב את שתי השיטות (ספקים מול קניות
בסופר). כל עוד זה לא מוכרע, כל מספר פוד-קוסט מוצג מנופח בכ-17%, וסף
ה"ירוק" של 32% נדלק בפועל ב-27%. **זו שאלה עסקית לנוי, לא החלטה טכנית.
תשאל אותה לפני שאתה משנה מספרים.**

---

## 9.2 שלושה פיצ'רים חדשים (19.8.2026)

**קישור מנה↔מתכון.** לכל מנה יש עכשיו `recipeId`, ובורר מתכון בעריכת מנה.
שינוי שם מנה **מעדכן אוטומטית את שם המתכון**. הקישור נוצר פעם אחת ל-מנות
הקיימות (`backfillRecipeLinks`) בזמן שהשמות עוד תאמו. **אל תחזור להשוואת
שמות** — היא נשארה רק כנפילה אחורה להזמנות ישנות.

**ייבוא מתכון מאקסל.** מטבח → מתכונים → "ייבוא מאקסל". נוי מדביקה שתי
עמודות (שם, גרמים). ה-parser מזהה טאבים, פסיקים, שורת כותרת ופסיק עשרוני,
ומתאים מול המזווה. **זה במכוון לא OCR מתמונה** — קריאת כתב יד עברי מצילום
לא אמינה, ותמחור על מספר שנקרא לא נכון גרוע מלא לתמחר.

**לשונית כספים.** הכנסות **מחושבות מההזמנות** — נוי לא מזינה אותן, אחרת
היו שתי מערכות מספרים שסותרות. היא מזינה רק הוצאות, עם צילום חשבונית.
הנתונים ב-`nuika/finance/entries` ו-`nuika/finance/photos` — **צמתים
נפרדים בכוונה**, כדי שרשימת החודש תיטען בלי לגרור כל תמונה.

> **תלות פתוחה:** הכללים ל-`nuika/finance` קיימים ב-`firebase-rules.json`
> אבל **חייבים להתפרסם ידנית בקונסול**. עד אז כל שמירה בלשונית כספים תיכשל
> עם "אין הרשאה". זו לא תקלה בקוד — אל תנסה לתקן אותה בקוד.

---

## 10. נתיבי הכסף — תבדוק אותם בכל נגיעה בהזמנות

**הסכום חייב להיות זהה בחמישה מקומות:** שורת העגלה, מודאל ההזמנה, הודעת
הוואטסאפ, דף התודה, והרשומה ב-Firebase. משתנה מיושן באחד מהם כבר שלח לאוויר
כפתור הזמנה מת.

**המלאי נגזר מההזמנות במחזור הנוכחי, לא מסטטוס ההזמנה.** אחרת סימון הזמנה
כ"נמסרה" מחזיר את היחידות שלה לחנות ומאפשר מכירת יתר.

קבועים רלוונטיים: `DELIVERY_COST = 25`, `MIN_ORDER = 100`,
`PICKUP_ADDRESS = 'ש. בן ציון 13, רחובות'`.

---

## 11. מודל האבטחה

הכניסה לניהול עוברת דרך **Firebase Authentication** — חשבון אמיתי, לא סיסמה
בקוד. אחרי הכניסה, הקוד בודק שה-UID רשום תחת `nuika/admins/{uid}`. כללי בסיס
הנתונים בודקים את אותו דבר.

- **החשבון של נוי:** `noynaamad@gmail.com`
- **שדה האימייל במסך הכניסה ממולא אוטומטית בכתובת של נוי.** אם מישהו אחר
  מנסה להיכנס עם הסיסמה שלו, Firebase יבדוק אותה מול החשבון של נוי וידחה —
  וההודעה תיראה כמו "סיסמה שגויה". **זו מלכודת אמיתית.** אם מישהו מדווח על
  סיסמה שגויה — הדבר הראשון לבדוק הוא איזה אימייל נמצא בשדה.
- **סיסמה שנשכחה:** הקישור "שכחתי סיסמה" במסך הכניסה. **לא** דרך עריכת קוד.
- `nuika/admins` נכתב **ידנית בקונסול של Firebase בלבד**. הכללים מגדירים
  `.write: false` — האפליקציה לא מסוגלת לכתוב לשם, בכוונה.
- הכללים מטפסים כל שדה בהזמנה. שדה לא מוכר **נדחה**. אם אתה מוסיף שדה
  להזמנה, תוסיף אותו גם ל-`firebase-rules.json` ותגיד לשהר לפרסם את הכללים.

---

## 12. מה כבר נבנה — אל תבנה מחדש

- דף נחיתה, תפריט, עגלה חיה, שליחת הזמנה בוואטסאפ, דף תודה
- פאנל ניהול מלא: מנות, מחירים, תמונות, אזל, בנק מנות
- ניהול הזמנות: סטטוסים (`new → confirmed → ready → done`), ארכיון אוטומטי
  אחרי 7 ימים, סינון, רשימת אפייה להדפסה, ייצוא CSV
- **הזמנה ידנית** — להזנת הזמנה שהתקבלה בטלפון או בוואטסאפ
- מטבח: מזווה, מתכונים, תמחור, תכנון שבועי
- מונה מלאי לפי מחזור שבועי
- אימות Firebase + כללי אבטחה מלאים
- PWA: התקנה למסך הבית, Service Worker, אייקונים
- תגיות OG לשיתוף בוואטסאפ
- מתג עברית/אנגלית
- `sync` / `ship` / `undo` / `validate` / `setup` + git hooks + בדיקה
  אוטומטית ב-GitHub Actions

---

## 13. באגים ידועים שעדיין לא תוקנו

- **תמונות מוצרים נשמרות כ-base64 בתוך `nuika/products`**, כך שכל מבקר מוריד
  אותן בכל טעינה. צריך להקטין לפני העלאה או לעבור ל-Firebase Storage.
- **מע"מ מקובע על 17%.** בישראל הוא 18% מינואר 2025. התמחור גם מניח שנוי
  עוסקת פטורה — שווה לוודא איתה.

---

## 14. חירום — האתר למטה או שבור

```bash
node scripts/undo.mjs
```

מציג את השינוי האחרון, שואל אם לבטל, ומחזיר את האתר אחורה תוך דקה. משתמש
ב-`git revert` — שום דבר לא נמחק וההיסטוריה נשמרת.

אם הבעיה עמוקה יותר — תעצור, אל תנחש, ותפנה לשהר. עדיף אתר שעובד בגרסה ישנה
מאתר שבור.

---

## 15. איך לדבר עם נוי

- **בעברית, במילים פשוטות.** בלי ז'רגון טכני ובלי שמות קבצים כשאפשר בלעדיהם.
- **תתאר מה יקרה, לא איך.** "המחיר ישתנה אצל כל הלקוחות" עדיף על "אעדכן את
  ה-node ב-Firebase".
- **אם היא מבקשת משהו מסוכן — תסביר למה, ותציע דרך אחרת.** אל תבצע בשקט
  ואל תסרב בלי הסבר.
- **אם משהו נכשל — תגיד את זה ישירות.** אל תרכך ואל תדווח על הצלחה חלקית
  כהצלחה.
- **תראה לה לפני שהיא שולחת.** "תפתחי לי את האתר לבדיקה" היא בקשה שהיא
  יודעת לעשות, וכדאי שתציע אותה בעצמך.

---

## 16. תמצית — אם תזכור רק את זה

1. **לעולם לא "Add files via upload" בגיטהאב.** זה כבר מחק שנה של עבודה,
   פעמיים.
2. **רק `sync.mjs` ו-`ship.mjs`.** אף פקודת גיט גולמית.
3. **`validate.mjs` חייב לעבור לפני כל שליחה.**
4. **בהתנגשות — למזג, אף פעם לא לקחת צד אחד שלם.**
5. **רוב הבקשות של נוי נפתרות בפאנל, בלי קוד.**
6. **זה עסק אמיתי עם הזמנות אמיתיות.** תבדוק לפני שאתה מדווח על הצלחה.
